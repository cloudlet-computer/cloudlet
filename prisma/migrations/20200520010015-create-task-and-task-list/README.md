# Migration `20200520010015-create-task-and-task-list`

This migration has been generated by Andy Mockler at 5/20/2020, 1:00:15 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."TaskList" (
"id" SERIAL,"title" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Task" (
"completedAt" timestamp(3)   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"taskListId" integer  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."TaskList" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Task" ADD FOREIGN KEY ("taskListId")REFERENCES "public"."TaskList"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200517100335-add-updated-at-to-notes..20200520010015-create-task-and-task-list
--- datamodel.dml
+++ datamodel.dml
@@ -1,19 +1,20 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id        Int      @default(autoincrement()) @id
-  username  String   @unique
-  createdAt DateTime @default(now())
+  id        Int        @default(autoincrement()) @id
+  username  String     @unique
+  createdAt DateTime   @default(now())
   notes     Note[]
   password  String
+  taskLists TaskList[]
 }
 model Note {
   id        Int      @default(autoincrement()) @id
@@ -22,4 +23,21 @@
   title     String
   user      User     @relation(fields: [userId], references: [id])
   userId    Int
 }
+
+model TaskList {
+  id     Int    @default(autoincrement()) @id
+  title  String
+  user   User   @relation(fields: [userId], references: [id])
+  userId Int
+  tasks  Task[]
+}
+
+model Task {
+  id          Int       @default(autoincrement()) @id
+  taskList    TaskList  @relation(fields: [taskListId], references: [id])
+  taskListId  Int
+  completedAt DateTime?
+  createdAt   DateTime  @default(now())
+  updatedAt   DateTime  @default(now()) @updatedAt
+}
```


